#ifndef Note_HPP
#define Note_HPP

#include <iostream>
#include <map>
#include <string>

using namespace std;

void play_note(string note, int duration);
int get_frequency(string note);
string list_notes();

#endif